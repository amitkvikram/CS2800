#include<stdio.h>
#include<limits.h>
#include<bool.h>

void merge(FILE *fp, int l, int r, int mid){
	

int main(){
	char filename[32];
	printf("Enter FileName: ");
	scanf("%s", FileName);
	fp = fopen(FileName, 'r');
	if(fp == NULL){
		printf("Error in Opening File");
		return 0;
	}
	
	
